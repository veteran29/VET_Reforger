{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Setup links",
            "type": "shell",
            "command": "powershell.exe -c '\"${workspaceFolder}\\.vscode\\scripts\\link.ps1\"' -workspace ${workspaceFolder}",
            "problemMatcher": []
        }
    ]
}
